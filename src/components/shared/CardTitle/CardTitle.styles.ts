import { buildStyleObject } from 'styles/theme';

export const title = buildStyleObject({
  display: 'block',
  cursor: 'pointer',
  fontSize: '1.5rem',
  fontWeight: 'bold',
  textDecoration: 'none',
});
