query GetAllTalks {
  talkCollection {
    items {
      title
      abstract {
        json
      }
      slug
      contentfulMetadata {
        tags {
          id
          name
        }
      }
    }
  }
}
