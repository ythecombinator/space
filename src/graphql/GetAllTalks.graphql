query GetAllTalks {
  talkCollection(order: lastRelevant_DESC) {
    items {
      title
      shortDescription {
        json
      }
      slug
      contentfulMetadata {
        tags {
          id
          name
        }
      }
    }
  }
}
