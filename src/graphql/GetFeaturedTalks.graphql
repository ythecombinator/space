query GetFeaturedTalks {
  sessionCollection(where: { photo_exists: true }) {
    items {
      talk {
        title
        slug
      }
      photo {
        url
      }
      event {
        name
        startingDate
        city {
          name
          country {
            name
          }
        }
      }
    }
  }
}
