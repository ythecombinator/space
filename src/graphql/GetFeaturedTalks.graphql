query GetFeaturedTalks {
  sessionCollection(where: { featured: true }) {
    items {
      talk {
        title
        slug
      }
      photo {
        url
      }
      event {
        name
        startingDate
        city {
          name
          country {
            name
          }
        }
      }
    }
  }
}
