query GetTalks($limit: Int) {
  talkCollection(order: lastRelevant_DESC, limit: $limit) {
    items {
      title
      shortDescription {
        json
      }
      slug
      contentfulMetadata {
        tags {
          id
          name
        }
      }
    }
  }
}
